package Strings_go;

public class stringBasics {
    static void main(String[] args) {
        String s="Vipin";
        System.out.println(s.charAt(2));//same as s[3] but in java index for string is not allowed..
        int n=s.length();
        System.out.println(n);
        String str ="1256345";
        int l=Integer.parseInt(str);//convert str to int
    }
}
